jclasslib bytecode viewer is a tool that visualizes all aspects of compiled Java class files and the contained bytecode.

jclasslib bytecode viewer doesnt need any dependency.

Install that using rpm.

cd /jclasslib_linux_4.3.1

rpm -ivh ./jclasslib_linux_4.3.1.rpm

Run the program by typing < jclasslib > in the terminal.

